server:
  port: 8081

spring:

  datasource:
    url: jdbc:postgresql://localhost:5433/bank_cards
    username: postgres
    password: korobov
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true


  mail:
    host: smtp.mail.ru
    port: 465
    username: alexkor0303@mail.ru
    password: ${EMAIL_PASSWORD}
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory



